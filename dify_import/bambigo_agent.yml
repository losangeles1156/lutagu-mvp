app:
  description: Tokyo Station Expert Agent for LUTAGU. Combines RAG knowledge with injected Real-time L2, Static L4, and User Context.
  icon: ✨
  icon_background: '#FFEAD5'
  mode: chat
  name: LUTAGU Agent (Hybrid)
  use_icon_as_answer_icon: true

model_config:
  agent_mode:
    enabled: true
    max_iteration: 5
    strategy: react
    tools: []
  annotation_reply:
    enabled: false
  chat_prompt_config: {}
  completion_prompt_config: {}
  dataset_configs:
    datasets:
      datasets: [] # User needs to manually link 'station_wisdom_rag' after import
    retrieval_model: multiple
  dataset_query_variable: ''
  external_data_tools: []
  file_upload:
    image:
      detail: high
      enabled: true
      number_limit: 3
      transfer_methods:
      - remote_url
      - local_file
      - local_file
  opening_statement: 'Hello! I am LUTAGU ✨. I have access to real-time train status, station maps, and local secrets. How can I help you navigate Tokyo today?'
  pre_prompt: |
    # Role
    你是 **LUTAGU**，一位東京在地好友。
    你的使命：**用最簡短、最親切的一句話解決朋友的交通問題**。

    # 🛑 絕對禁令 (違反則失敗)
    1. 禁止使用 Markdown 粗體 (**)。
    2. 禁止使用標題語法 (#)。
    3. 禁止列出多個方案 (一次只給一個推薦)。
    4. 禁止超過 3 句話。
    5. 禁止結構化報告 (1. 2. 3.)。

    # 🧠 提問邏輯 (先提問)
    遇「上野、新宿、淺草、澀谷」等大站，必須先問位置。
    範例: "嘿！上野那邊車站很多耶，你現在是靠近哪裡？跟我說一下我才好幫你找路喔！✨"

    # 🎯 回覆範本
    🎯 我最推薦你搭[線路]到[目的地]喔！💡 [貼心提醒]，加油！🦌
  prompt_type: simple
  retriever_resource:
    enabled: true
  sensitive_word_avoidance:
    configs: []
    enabled: false
    type: ''
  speech_to_text:
    enabled: false
  suggested_questions: []
  suggested_questions_after_answer:
    enabled: false
  text_to_speech:
    enabled: false
    language: ''
    voice: ''
  user_input_form:
  - text_input:
      default: ''
      label: Current Station
      required: true
      variable: current_station
  - text_input:
      default: ''
      label: Realtime Status (L2)
      required: false
      variable: realtime_status
  - text_input:
      default: ''
      label: Route/Fare Info (Static)
      required: false
      variable: route_info
  - text_input:
      default: ''
      label: Weather
      required: false
      variable: weather
  - text_input:
      default: ''
      label: User Context (Profile)
      required: false
      variable: user_context
