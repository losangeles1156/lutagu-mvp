# ⚡ 立即測試 - 樞紐站標籤修正

## 🚨 重要：已發現並修正根本原因！

**問題**：Icon cache key 缺少 `zoom` 依賴，導致舊 icon 被重複使用

**修正**：
1. ✅ Cache key 加入 `zoom`
2. ✅ `showLabel` 使用 `useMemo`

---

## 📋 測試步驟（3 步驟）

### Step 1: 完全重新載入頁面

**⚠️ 關鍵：必須清除舊 cache！**

選擇以下任一方法：

**方法 A：硬重新整理（最快）**
```
Mac: Cmd + Shift + R
Windows: Ctrl + Shift + F5
```

**方法 B：關閉分頁重開**
1. 關閉 localhost:3001 分頁
2. 重新開啟 http://localhost:3001

---

### Step 2: 立即檢查

開啟頁面後，**直接看地圖**，應該看到：

✅ **樞紐站（藍色 M）顯示站名**
```
   [M]
   上野
   +2
```

❌ **不應該只有數字**
```
   [M]
   +2    ← 這是問題（舊版本）
```

---

### Step 3: Zoom 測試（可選）

**縮小地圖**（Zoom Out −）：
- 樞紐站名稱**仍然顯示** ✅

**放大地圖**（Zoom In +）：
- 更多站點出現，標籤也顯示 ✅

---

## ✅ 成功標準

- [ ] 上野、秋葉原、東京等樞紐站顯示站名
- [ ] 站名在所有 Zoom 級別都可見
- [ ] 標籤與 marker 對齊，清晰可讀

---

## 🐛 如果仍然看不到

### 快速除錯

在瀏覽器 Console（F12）貼上：

```javascript
const markers = document.querySelectorAll('.custom-node-icon');
const withLabels = Array.from(markers).filter(m =>
  m.querySelector('.absolute.-bottom-12')
).length;
console.log(`Labels: ${withLabels}/${markers.length}`);
```

**預期輸出**：`Labels: 20+/40+`（至少 50% 有標籤）

**如果 < 10%**：請提供截圖和 Console 輸出

---

## 📞 回報格式

如果測試通過：
> ✅ 測試通過！樞紐站名稱已顯示

如果仍有問題：
> ❌ 仍看不到，Console 輸出：[貼上結果]
> [附上截圖]

---

**測試時間**：< 1 分鐘
**關鍵動作**：硬重新整理（Cmd+Shift+R）
