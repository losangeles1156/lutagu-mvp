use anyhow::Result;

pub async fn run(amenity: &str, radius: u32, workers: usize) -> Result<()> {
    println!("FillOsm not implemented yet. Amenity: {}, r={}, w={}", amenity, radius, workers);
    Ok(())
}
