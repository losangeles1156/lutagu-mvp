'use client';

// Re-export the unified ErrorBoundary from ui/ErrorBoundary
// This ensures backward compatibility for imports from common/ErrorBoundary
export { ErrorBoundary } from '../ui/ErrorBoundary';
export { default } from '../ui/ErrorBoundary';
